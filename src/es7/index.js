let fruits = [1,5,96,2,3,4,9];
fruits.includes(9) 
    ? console.log('Incluido') 
    : console.log('No Incluido');

//Elevar a la potencia Ej: 2^3
let base = 2;
let exponent = 3;
let result = base ** exponent;
console.log(result);
// Antes Math.pow(base, exponente)
console.log(Math.pow(2, 4)); // 16